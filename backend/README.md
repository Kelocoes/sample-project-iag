# NestJS Backend Service

Servicio Backend REST desarrollado en NestJS con TypeORM y SQLite (`database.sqlite`).

## Ejecución

```bash
npm install
npm run build
node dist/main.js
```

El servidor responde en `http://localhost:3000/api`.
